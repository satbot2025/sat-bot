{
  "name": "sat-bot",
  "version": "0.1.0",
  "private": true,
  "description": "SAT Bot frontend (TypeScript) + Flask backend",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
